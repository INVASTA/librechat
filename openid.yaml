version: "1.2.8"
cache: true

# =============================================================================
# НАСТРОЙКИ ИНТЕРФЕЙСА
# =============================================================================
interface:
  privacyPolicy:
    externalUrl: "https://your-privacy-policy-url.com"
    openNewTab: true
  termsOfService:
    externalUrl: "https://your-terms-url.com"
    openNewTab: true

# =============================================================================
# КОНФИГУРАЦИЯ ENDPOINTS (ОБЯЗАТЕЛЬНО ДОЛЖЕН БЫТЬ ХОТЯ БЫ ОДИН ПРОВАЙДЕР)
# =============================================================================
endpoints:
  openai:
    enabled: false   # Отключаем, если не используется

# =============================================================================
# OpenID CONNECT (НАСТРОЙКИ ТОЛЬКО ЧЕРЕЗ ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ!)
# =============================================================================
# Все эти параметры должны быть в переменных окружения:
# OPENID_ISSUER, OPENID_CLIENT_ID, OPENID_CLIENT_SECRET
# OPENID_CALLBACK_URL, OPENID_SCOPE и т.д.
