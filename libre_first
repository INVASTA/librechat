# =============================================================================
# LIBRECHAT CONFIGURATION (v1.2.8)
# =============================================================================
version: "1.2.8"
appTitle: "LibreChat"
cache: false

# =============================================================================
# INTERFACE SETTINGS
# =============================================================================
interface:
  privacyPolicy:
    enabled: true
    externalUrl: "https://your-privacy-policy-url.com"
    openNewTab: true
  termsOfService:
    enabled: true
    externalUrl: "https://your-terms-url.com"
    openNewTab: true

# =============================================================================
# FILE UPLOAD CONFIGURATION
# =============================================================================
fileConfig:
  enabled: true
  serverUpload: false
  endpoints:
    default:
      fileLimit: 5
      fileSizeLimit: 10  # MB
      totalSizeLimit: 50 # MB

# =============================================================================
# ENDPOINTS CONFIGURATION (ONLY GROK)
# =============================================================================
endpoints:
  - type: "openai"
    enabled: false
  - type: "anthropic"
    enabled: false
  - type: "google"
    enabled: false
  - type: "custom"
    name: "Grok"
    apiKey: "${XAI_API_KEY}"
    baseURL: "https://api.x.ai/v1"
    models:
      default: "grok-4-0709"
      fetch: false
    titleConvo: true
    titleModel: "Grok"
    headers:
      "Content-Type": "application/json"

# =============================================================================
# AUTHENTICATION (OpenID Connect)
# =============================================================================
auth:
  openid:
    enabled: true
    issuer: "${OPENID_ISSUER}"
    clientId: "${OPENID_CLIENT_ID}"
    clientSecret: "${OPENID_CLIENT_SECRET}"
    sessionSecret: "${OPENID_SESSION_SECRET}"
    callbackUrl: "${OPENID_CALLBACK_URL}"
    scope: "openid profile email"
    buttonLabel: "Login with OpenID"
    imageUrl: "https://example.com/openid-logo.png"
    useEndSessionEndpoint: true

# =============================================================================
# RATE LIMITS
# =============================================================================
rateLimits:
  enabled: true
  fileUploads:
    ipMax: 100
    ipWindowInMinutes: 60
    userMax: 50
    userWindowInMinutes: 60
